'use client'

import { useMemo, useState } from 'react'
import { BookOpen, Search, ShoppingBag, UserRound, Menu, ChevronRight, Star, Plus, Minus, X, LayoutDashboard, Package, Users, Settings, ArrowUpRight, MoreHorizontal, CheckCircle2 } from 'lucide-react'

type Book = { id: number; title: string; author: string; genre: string; price: number; rating: number; color: string; tag?: string; stock: number }

const books: Book[] = [
  { id: 1, title: 'The Atlas of Quiet Places', author: 'Mira Ellison', genre: 'Literary Fiction', price: 24, rating: 4.8, color: 'clay', tag: 'Staff pick', stock: 24 },
  { id: 2, title: 'A Field Guide to Wonder', author: 'Jon Bellamy', genre: 'Nature & Science', price: 28, rating: 4.9, color: 'moss', tag: 'New release', stock: 18 },
  { id: 3, title: 'Letters from the Coast', author: 'Iris Maren', genre: 'Memoir', price: 21, rating: 4.7, color: 'sky', stock: 32 },
  { id: 4, title: 'The Art of Starting Over', author: 'Nora Finch', genre: 'Self Development', price: 19, rating: 4.6, color: 'gold', stock: 9 },
  { id: 5, title: 'Small Fires, Long Shadows', author: 'Theo Ash', genre: 'Poetry', price: 17, rating: 4.8, color: 'plum', stock: 14 },
  { id: 6, title: 'The Cartographer’s Daughter', author: 'Elena Voss', genre: 'Historical Fiction', price: 26, rating: 4.9, color: 'ink', stock: 21 },
]

const navItems = ['Discover', 'Books', 'Collections', 'About us']

export default function BookstoreApp() {
  const [view, setView] = useState<'home' | 'books' | 'admin'>('home')
  const [query, setQuery] = useState('')
  const [genre, setGenre] = useState('All genres')
  const [cart, setCart] = useState<number[]>([1])
  const [showCart, setShowCart] = useState(false)
  const [showAuth, setShowAuth] = useState(false)
  const [toast, setToast] = useState('')
  const [page, setPage] = useState(1)

  const filtered = useMemo(() => books.filter((book) => (genre === 'All genres' || book.genre === genre) && `${book.title} ${book.author}`.toLowerCase().includes(query.toLowerCase())), [query, genre])
  const cartBooks = cart.map((id) => books.find((book) => book.id === id)!).filter(Boolean)
  const total = cartBooks.reduce((sum, book) => sum + book.price, 0)
  const notify = (message: string) => { setToast(message); window.setTimeout(() => setToast(''), 2600) }
  const addToCart = (id: number) => { setCart((current) => [...current, id]); notify('Book added to your bag') }

  return <div className="min-h-screen bg-background text-foreground">
    <header className="border-b border-border bg-background/95 backdrop-blur-sm sticky top-0 z-30">
      <div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 lg:px-8">
        <button onClick={() => setView('home')} className="flex items-center gap-3" aria-label="The Margin home"><span className="grid size-9 place-items-center rounded-full bg-primary text-primary-foreground"><BookOpen className="size-4" /></span><span className="font-serif text-xl font-semibold tracking-tight">The Margin</span></button>
        <nav className="hidden items-center gap-8 md:flex">{navItems.map((item) => <button key={item} onClick={() => setView(item === 'Books' ? 'books' : 'home')} className="text-sm text-muted-foreground transition hover:text-foreground">{item}</button>)}</nav>
        <div className="flex items-center gap-2"><button onClick={() => setView('admin')} className="hidden rounded-full px-3 py-2 text-xs font-medium text-muted-foreground hover:bg-secondary md:block">Admin workspace</button><button onClick={() => setShowAuth(true)} className="grid size-9 place-items-center rounded-full hover:bg-secondary" aria-label="Account"><UserRound className="size-4" /></button><button onClick={() => setShowCart(true)} className="relative grid size-9 place-items-center rounded-full hover:bg-secondary" aria-label="Shopping bag"><ShoppingBag className="size-4" />{cart.length > 0 && <span className="absolute right-0 top-0 grid size-4 place-items-center rounded-full bg-primary text-[10px] text-primary-foreground">{cart.length}</span>}</button><button className="grid size-9 place-items-center rounded-full md:hidden" aria-label="Menu"><Menu className="size-5" /></button></div>
      </div>
    </header>

    {view === 'admin' ? <AdminView onBack={() => setView('home')} notify={notify} /> : view === 'home' ? <HomeView onBrowse={() => setView('books')} addToCart={addToCart} /> : <CatalogView query={query} setQuery={setQuery} genre={genre} setGenre={setGenre} filtered={filtered} page={page} setPage={setPage} addToCart={addToCart} />}

    <footer className="mt-20 border-t border-border"><div className="mx-auto flex max-w-7xl flex-col gap-5 px-5 py-8 text-sm text-muted-foreground md:flex-row md:items-center md:justify-between lg:px-8"><span className="font-serif text-lg text-foreground">The Margin</span><span>Independent books for curious minds.</span><span>© 2026 The Margin Bookshop</span></div></footer>
    {showCart && <CartDrawer books={cartBooks} total={total} close={() => setShowCart(false)} checkout={() => { setShowCart(false); notify('Order placed — thank you for reading') }} />}
    {showAuth && <AuthModal close={() => setShowAuth(false)} notify={notify} />}
    {toast && <div role="status" className="fixed bottom-5 left-1/2 z-50 flex -translate-x-1/2 items-center gap-2 rounded-full bg-primary px-5 py-3 text-sm text-primary-foreground shadow-lg"><CheckCircle2 className="size-4" />{toast}</div>}
  </div>
}

function HomeView({ onBrowse, addToCart }: { onBrowse: () => void; addToCart: (id: number) => void }) { return <main><section className="mx-auto grid max-w-7xl gap-10 px-5 pb-16 pt-16 lg:grid-cols-[1.1fr_.9fr] lg:items-center lg:px-8 lg:pb-24 lg:pt-24"><div><p className="mb-5 font-mono text-xs uppercase tracking-[.22em] text-primary">Independent bookshop · Est. 2018</p><h1 className="max-w-2xl font-serif text-5xl leading-[.98] tracking-tight text-balance md:text-7xl">Books for the <em className="text-primary">in-between</em> moments.</h1><p className="mt-7 max-w-lg text-base leading-7 text-muted-foreground">Thoughtfully chosen stories, ideas, and objects for slow mornings, long journeys, and the questions that stay with you.</p><div className="mt-8 flex flex-wrap gap-3"><button onClick={onBrowse} className="inline-flex items-center gap-2 rounded-full bg-primary px-5 py-3 text-sm font-medium text-primary-foreground hover:opacity-90">Explore the collection <ChevronRight className="size-4" /></button><button className="rounded-full border border-border px-5 py-3 text-sm font-medium hover:bg-secondary">Our story</button></div></div><div className="relative min-h-[360px] overflow-hidden rounded-[2rem] bg-primary p-8 text-primary-foreground md:min-h-[440px]"><div className="absolute right-[-8%] top-[12%] h-64 w-52 rotate-[12deg] rounded-sm bg-card p-5 text-card-foreground shadow-2xl"><div className="flex h-full flex-col justify-between border border-primary/20 p-4"><span className="font-mono text-[9px] uppercase tracking-widest">Vol. 04</span><span className="font-serif text-2xl leading-tight">The Atlas of<br /><i>Quiet Places</i></span><span className="text-xs">Mira Ellison</span></div></div><div className="absolute bottom-8 left-8 max-w-[180px]"><p className="font-mono text-[10px] uppercase tracking-widest opacity-70">Featured this month</p><p className="mt-2 font-serif text-2xl leading-tight">A softer way to see the world.</p></div><div className="absolute bottom-[-30px] right-32 h-40 w-32 rotate-[-18deg] rounded-sm bg-accent p-3 text-accent-foreground shadow-xl"><div className="h-full border border-current/20" /></div></div></section><section className="border-y border-border bg-secondary/40"><div className="mx-auto flex max-w-7xl flex-col gap-4 px-5 py-5 text-sm md:flex-row md:items-center md:justify-between lg:px-8"><span className="font-medium">Free shipping on orders over $50</span><span className="text-muted-foreground">Curated by readers, for readers</span><span className="text-muted-foreground">New titles every Tuesday</span></div></section><section className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><div className="flex items-end justify-between"><div><p className="font-mono text-xs uppercase tracking-[.2em] text-primary">The shortlist</p><h2 className="mt-2 font-serif text-4xl">Staff favorites</h2></div><button onClick={onBrowse} className="hidden items-center gap-1 text-sm font-medium md:flex">View all <ArrowUpRight className="size-4" /></button></div><div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{books.slice(0,4).map((book) => <BookCard key={book.id} book={book} addToCart={addToCart} />)}</div></section></main> }

function BookCard({ book, addToCart }: { book: Book; addToCart: (id: number) => void }) { return <article className="group"><div className={`book-cover ${book.color} relative aspect-[4/5] overflow-hidden rounded-xl p-5`}><div className="flex h-full flex-col justify-between border border-current/20 p-3"><span className="font-mono text-[9px] uppercase tracking-widest opacity-70">The Margin</span><span className="font-serif text-xl leading-tight">{book.title}</span><span className="text-xs opacity-75">{book.author}</span></div>{book.tag && <span className="absolute left-3 top-3 rounded-full bg-background/80 px-2 py-1 font-mono text-[9px] uppercase tracking-wider">{book.tag}</span>}</div><div className="flex items-start justify-between gap-3 pt-4"><div><h3 className="font-serif text-lg leading-tight">{book.title}</h3><p className="mt-1 text-sm text-muted-foreground">{book.author}</p><div className="mt-2 flex items-center gap-1 text-xs text-muted-foreground"><Star className="size-3 fill-current text-primary" /> {book.rating}</div></div><button onClick={() => addToCart(book.id)} className="mt-1 grid size-9 shrink-0 place-items-center rounded-full border border-border opacity-0 transition group-hover:opacity-100 hover:bg-primary hover:text-primary-foreground" aria-label={`Add ${book.title} to bag`}><Plus className="size-4" /></button></div></article> }

function CatalogView({ query, setQuery, genre, setGenre, filtered, page, setPage, addToCart }: any) { const genres = ['All genres', ...Array.from(new Set(books.map((b) => b.genre)))]; return <main className="mx-auto max-w-7xl px-5 py-12 lg:px-8"><div className="flex flex-col justify-between gap-6 border-b border-border pb-8 md:flex-row md:items-end"><div><p className="font-mono text-xs uppercase tracking-[.2em] text-primary">The library</p><h1 className="mt-2 font-serif text-5xl">Find your next read.</h1></div><div className="relative w-full md:w-72"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search titles or authors" className="w-full rounded-full border border-border bg-background py-3 pl-10 pr-4 text-sm outline-none focus:ring-2 focus:ring-ring" /></div></div><div className="flex gap-2 overflow-x-auto py-6">{genres.map((item) => <button key={item} onClick={() => setGenre(item)} className={`whitespace-nowrap rounded-full border px-4 py-2 text-xs ${genre === item ? 'border-primary bg-primary text-primary-foreground' : 'border-border hover:bg-secondary'}`}>{item}</button>)}</div><div className="grid gap-x-5 gap-y-12 sm:grid-cols-2 lg:grid-cols-4">{filtered.map((book: Book) => <BookCard key={book.id} book={book} addToCart={addToCart} />)}</div>{filtered.length === 0 && <p className="py-16 text-center text-muted-foreground">No books found. Try another search.</p>}<div className="mt-14 flex items-center justify-center gap-2"><button onClick={() => setPage(Math.max(1, page - 1))} className="rounded-full border border-border px-4 py-2 text-sm">Previous</button><span className="px-3 text-sm text-muted-foreground">Page {page} of 2</span><button onClick={() => setPage(Math.min(2, page + 1))} className="rounded-full border border-border px-4 py-2 text-sm">Next</button></div></main> }

function CartDrawer({ books, total, close, checkout }: { books: Book[]; total: number; close: () => void; checkout: () => void }) { return <div className="fixed inset-0 z-40 bg-foreground/30" onClick={close}><aside onClick={(e) => e.stopPropagation()} className="absolute right-0 top-0 flex h-full w-full max-w-md flex-col bg-background p-6 shadow-2xl"><div className="flex items-center justify-between"><h2 className="font-serif text-3xl">Your bag</h2><button onClick={close} className="grid size-9 place-items-center rounded-full hover:bg-secondary" aria-label="Close cart"><X className="size-5" /></button></div><div className="mt-8 flex flex-1 flex-col gap-5">{books.map((book) => <div key={book.id} className="flex gap-4"><div className={`book-cover ${book.color} h-24 w-20 shrink-0 rounded-lg p-2`}><div className="h-full border border-current/20" /></div><div className="flex flex-1 justify-between"><div><p className="font-serif text-lg leading-tight">{book.title}</p><p className="mt-1 text-sm text-muted-foreground">{book.author}</p><div className="mt-3 flex items-center gap-3"><button className="grid size-6 place-items-center rounded-full border border-border"><Minus className="size-3" /></button><span className="text-sm">1</span><button className="grid size-6 place-items-center rounded-full border border-border"><Plus className="size-3" /></button></div></div><span className="font-medium">${book.price}</span></div></div>)}{books.length === 0 && <p className="text-muted-foreground">Your bag is waiting for a good story.</p>}</div><div className="border-t border-border pt-5"><div className="flex justify-between text-sm text-muted-foreground"><span>Subtotal</span><span>${total}</span></div><div className="mt-2 flex justify-between font-serif text-xl"><span>Total</span><span>${total}</span></div><button onClick={checkout} disabled={!books.length} className="mt-5 w-full rounded-full bg-primary py-3 text-sm font-medium text-primary-foreground disabled:opacity-40">Continue to checkout</button></div></aside></div> }

function AuthModal({ close, notify }: { close: () => void; notify: (s: string) => void }) { return <div className="fixed inset-0 z-40 grid place-items-center bg-foreground/30 p-5" onClick={close}><div onClick={(e) => e.stopPropagation()} className="w-full max-w-md rounded-2xl bg-background p-7 shadow-2xl"><div className="flex items-center justify-between"><div><p className="font-mono text-xs uppercase tracking-widest text-primary">Welcome back</p><h2 className="mt-2 font-serif text-3xl">Sign in to The Margin</h2></div><button onClick={close} aria-label="Close sign in"><X className="size-5" /></button></div><div className="mt-7 flex flex-col gap-4"><label className="text-sm">Email<input type="email" placeholder="you@example.com" className="mt-2 w-full rounded-lg border border-border bg-background p-3 outline-none focus:ring-2 focus:ring-ring" /></label><label className="text-sm">Password<input type="password" placeholder="••••••••" className="mt-2 w-full rounded-lg border border-border bg-background p-3 outline-none focus:ring-2 focus:ring-ring" /></label><button onClick={() => { close(); notify('Signed in successfully') }} className="mt-2 rounded-full bg-primary py-3 text-sm font-medium text-primary-foreground">Sign in</button><p className="text-center text-xs text-muted-foreground">New here? <span className="text-foreground underline">Create an account</span></p></div></div></div> }

function AdminView({ onBack, notify }: { onBack: () => void; notify: (s: string) => void }) { return <main className="min-h-[70vh] bg-secondary/30"><div className="mx-auto max-w-7xl px-5 py-10 lg:px-8"><div className="flex flex-col gap-5 md:flex-row md:items-end md:justify-between"><div><button onClick={onBack} className="mb-5 text-sm text-muted-foreground hover:text-foreground">← Back to storefront</button><p className="font-mono text-xs uppercase tracking-[.2em] text-primary">Admin workspace</p><h1 className="mt-2 font-serif text-5xl">Good morning, Alex.</h1></div><button onClick={() => notify('New book form opened')} className="inline-flex items-center justify-center gap-2 rounded-full bg-primary px-5 py-3 text-sm text-primary-foreground"><Plus className="size-4" /> Add a book</button></div><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[['Total books','1,284','+12 this month'],['Orders this week','86','+18.2%'],['Revenue',' $8,492','+9.4%'],['Low stock','14','Needs attention']].map(([label, value, delta]) => <div key={label} className="rounded-xl border border-border bg-background p-5"><p className="text-sm text-muted-foreground">{label}</p><p className="mt-3 font-serif text-3xl">{value}</p><p className="mt-2 text-xs text-primary">{delta}</p></div>)}</div><div className="mt-8 grid gap-6 lg:grid-cols-[1.4fr_.8fr]"><div className="rounded-xl border border-border bg-background"><div className="flex items-center justify-between border-b border-border p-5"><div><h2 className="font-serif text-2xl">Inventory</h2><p className="mt-1 text-sm text-muted-foreground">Manage your catalog and stock levels.</p></div><button className="rounded-full border border-border p-2"><MoreHorizontal className="size-4" /></button></div><div className="divide-y divide-border">{books.slice(0,5).map((book) => <div key={book.id} className="flex items-center gap-4 p-4"><div className={`book-cover ${book.color} size-12 shrink-0 rounded-md p-1`}><div className="h-full border border-current/20" /></div><div className="min-w-0 flex-1"><p className="truncate font-medium">{book.title}</p><p className="text-xs text-muted-foreground">{book.genre}</p></div><span className={`text-sm ${book.stock < 12 ? 'text-destructive' : 'text-muted-foreground'}`}>{book.stock} in stock</span><button onClick={() => notify(`Editing ${book.title}`)} className="rounded-full border border-border px-3 py-1.5 text-xs">Edit</button></div>)}</div></div><div className="rounded-xl border border-border bg-background p-5"><h2 className="font-serif text-2xl">Recent orders</h2><div className="mt-5 flex flex-col gap-5">{['#TM-1048 · Jamie L.',' #TM-1047 · Sol A.','#TM-1046 · Priya K.','#TM-1045 · Max R.'].map((order, i) => <div key={order} className="flex items-center justify-between"><div><p className="text-sm">{order}</p><p className="mt-1 text-xs text-muted-foreground">{i + 1} item · Today</p></div><span className="rounded-full bg-accent px-2.5 py-1 text-xs text-accent-foreground">{i === 0 ? 'Processing' : 'Shipped'}</span></div>)}</div><button className="mt-7 flex items-center gap-1 text-sm font-medium">View all orders <ArrowUpRight className="size-4" /></button></div></div></div></main> }
